<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Cocktail Pro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&family=Playfair+Display:ital,wght@0,400..900;1,400..900&family=Satisfy&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
</head>

<body>
    <div class="container p-4 mt-3 text-center text-lg-start">
        <div class="row">
            <div class="col-lg-6 col-12 text-center">
                <h1 class="text-center">
                    <%= name %>
                </h1>
                <img class="img-fluid" src="<%= thumb %>/preview" alt="">
                <div class="p-3" id="info">
                    <h6>
                        <%= alcoholic %>
                    </h6>
                    <h6>
                        <%= category %>
                    </h6>
                    <h6>Served with <%= glass %>
                    </h6>
                    <!-- <h6>Id: <%= id %>
                    </h6> -->
                    <a href="/" class="btn btn-danger mt-3">🍸 Random Cocktail 🍸</a>
                    <form class="search d-flex flex-wrap py-2" action="/search" method="post">
                        <div class="dropdown-center mx-auto mt-3">
                            <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Alcoholic?
                            </button>
                            <ul class="dropdown-menu scrollable-menu bg-warning">
                                <% for( let i=0; i < category_a.length; i++ ) { %>
                                    <li><button class="dropdown-item" name="a" type="submit"
                                            value=<%= encodeURIComponent(category_a[i]) %>>
                                            <%= category_a[i] %>
                                        </button></li>
                                    <% } %>
                            </ul>
                        </div>
                        <div class="dropdown-center mx-auto mt-3">
                            <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Category?
                            </button>
                            <ul class="dropdown-menu scrollable-menu bg-warning">
                                <% for( let i=0; i < category_c.length; i++ ) { %>
                                    <li><button class="dropdown-item" name="c" type="submit"
                                            value="<%= encodeURIComponent(category_c[i]) %>">
                                            <%= category_c[i] %>
                                        </button></li>
                                    <% } %>
                            </ul>
                        </div>
                        <div class="dropdown-center mx-auto mt-3">
                            <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Glass?
                            </button>
                            <ul class="dropdown-menu scrollable-menu bg-warning">
                                <% for( let i=0; i < category_g.length; i++ ) { %>
                                    <li><button class="dropdown-item" name="g" type="submit"
                                            value="<%= encodeURIComponent(category_g[i]) %>">
                                            <%= category_g[i] %>
                                        </button></li>
                                    <% } %>
                            </ul>
                        </div>
                        <div class="dropdown-center mx-auto mt-3">
                            <button class="btn btn-warning dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Ingredients?
                            </button>
                            <ul class="dropdown-menu scrollable-menu bg-warning">
                                <% for( let i=0; i < category_i.length; i++ ) { %>
                                    <li><button class="dropdown-item" name="i" type="submit"
                                            value="<%= encodeURIComponent(category_i[i]) %>">
                                            <%= category_i[i] %>
                                        </button></li>
                                    <% } %>
                            </ul>
                        </div>
                    </form>
                    <form action="/searchbar" method="post">
                        <div class="input-group">
                            <input class="form-control" type="text" name="s" placeholder="Search?">
                            <button class="btn btn-warning" type="submit">Search 🔍</button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="col-lg-6 col-12 px-3">
                <div id="instructions">
                    <h2>Instructions</h2>
                    <p>
                        <%= instructions %>
                    </p>
                </div>
                <div id="ingredients">
                    <h2 class="mt-3">Ingredients</h2>
                    <ul>
                        <% for( let i=0; i < ingredients.length; i++ ) { %>
                            <li class="text-lg-start">
                                <%= ingredients[i] %>
                            </li>
                            <% } %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
            <p class="col-md-4 mb-0 text-body-secondary">© 2024 Cocktail Pro</p>

            <a href="/"
                class="col-md-4 d-flex align-items-center justify-content-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
                <svg class="bi me-2" width="40" height="32">
                    <use xlink:href="#bootstrap"></use>
                </svg>
            </a>

            <ul class="nav col-md-4 justify-content-end">
                <li class="nav-item"><a href="https://www.alidarcan.com/"
                        class="nav-link px-2 text-body-secondary">Developer</a></li>
            </ul>
        </footer>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>